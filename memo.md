claude code 用devcontainerのテンプレート

.claude/settings.local.json と .devcontainer/devcontainer.json 内の
{sample-devcontainer} ← この文字列を丸ごと各プロジェクト名に置換して使う

個人用だからリモートに上げてるだけで、settings.local.jsonは本来個人用なのでプロジェクトのリポジトリにはpushしないように注意
